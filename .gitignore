# ไฟล์ต้นทาง Excel — ไม่ต้องขึ้นเว็บ (เว็บใช้แค่ data.json ที่แปลงแล้ว)
*.xlsx
*.xls

# ไฟล์ทำงานตอนดึงเส้นแม่น้ำจาก Overpass (ไม่ต้องขึ้นเว็บ)
overpass.ql
overpass_other.ql
osm_raw.json
osm_rivers_all.json
overpass_result.txt
ov_other_result.txt
tasks_out.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
Thumbs.db
.DS_Store
